mutation createIntent (
  $title: String!
  $values: [String!],
  $projectId: String!
) {
  createIntent (
    title: $title
    values: $values
    projectId: $projectId
  ) {
    id
    title
    values
  }
}