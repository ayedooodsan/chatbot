query myTrainings (
  $projectId: String!
  $type: String!
  $limit: Int
  $offset: Int
  $keyword: String
) {
  myTrainings (
    projectId: $projectId
    type: $type
    limit: $limit
    offset: $offset
    keyword: $keyword
  ) {
    trainings {
    	id
      title
      request
      noMatch
      createdAt
    }
    pageInfo {
      total
    }
  }
}