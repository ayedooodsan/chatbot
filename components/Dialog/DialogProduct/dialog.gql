query dialog (
  $id: String!
) {
  dialog (
    id: $id
  ) {
    title
    messages {
      id
      parentId
      type
      title
      intent {
        id
        title
        values {
          text
        }
      }
      payload
      depth
    }
  }
}